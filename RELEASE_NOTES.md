# 托盘管理 1.1.7

个人原创的独立工具，根据自己的审美和使用习惯制作，不是其他软件的修改版或 Fork，没有对应的“官方原版”。仍为实验项目，与 Microsoft 及被管理应用无隶属或背书关系。

## 本次更新

- 应用列表统一为 68 DIP 行高，路径单行省略，悬停显示完整路径。
- 修复高 DPI / 200% 缩放下的字号继承与重复放大；按可用空间调整布局。
- 自动检测 Windows Xbox 大屏体验，动态隐藏／恢复标题栏按钮；保留手柄操作。
- 隐藏右上角手动大屏按钮，保留 F11 手动切换。
- 可配置打开窗口的全局快捷键；单实例、退出程序入口继续保留。
- 改善应用友好名称、图标备用读取和浅色图标对比度。
- 标题栏按钮背景融入窗口并跟随深浅主题，修复发布包缺失 WinUI 样式资源。

## 下载和安装

下载 `TrayManager-1.1.7-win-x64.zip`，解压后运行 `Install.ps1`，或直接运行 `TrayManager.exe`。
升级前在旧版本中点击“退出程序”。设置保留在用户目录，不包含在分发包内。
需要 Windows 11 x64、.NET 10 Runtime 和 Windows App Runtime 2，采用共享运行库以缩小包体。

## 限制

不保证列出所有系统托盘图标。开关表示本工具规则，不是系统原有隐藏状态。
尚无正式卸载器或卸载自动恢复；删除程序前正常退出，异常终止不能保证恢复图标。
27 项底层与布局逻辑测试通过；完整手柄硬件及所有 DPI/显示器组合未穷尽验证。
