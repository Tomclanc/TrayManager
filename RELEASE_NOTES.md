# 托盘管理 1.0.2 · 个人实验版

根据个人审美和使用习惯从头制作的独立工具，不是其他软件的修改版或 Fork，没有对应的“官方原版”。目前仍处于实验阶段，请留意下方兼容性和使用限制。

- WinUI 3、Mica、圆角卡片和系统深浅色适配。
- 实时托盘列表、搜索、按项隐藏／恢复，不停止后台应用。
- 自身图标可隐藏、双击打开、单实例，不提供右键菜单。
- 修正隐藏后图标从列表消失的问题；保存并验证进程启动时间、窗口和图标身份。
- 15 项底层检查通过，本机实时扫描可识别 PowerToys Awake。

## 下载与运行

下载 `TrayManager-1.0.2-win-x64.zip`，解压运行 `TrayManager.exe`。包内提供 `Install.ps1`，可安装到当前用户目录并创建开始菜单入口。ZIP 约 9.8 MiB，需要 Windows 11 x64、.NET 10 Runtime、Windows App Runtime 2，不是自包含安装程序。

`SHA256SUMS.txt` 提供文件校验值。

## 已知限制

- 不保证覆盖全部系统及第三方托盘图标；图标重建时可能短暂出现。
- 开关不是系统原有隐藏状态，请避免与其他隐藏工具重复管理。
- **尚未实现正式卸载器或卸载时自动恢复。** 删除前应点击“退出程序”；恢复失败时请重启对应应用。不要直接删除运行中的程序。
- 已有无法定位的旧隐藏规则会保留并显示提示，不会猜测窗口身份进行操作。
