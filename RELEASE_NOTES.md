# 托盘管理 1.1.8

个人原创独立工具，根据自己的审美和使用习惯制作；不是其他软件的修改版或 Fork，没有对应的“官方原版”。仍为实验项目，与 Microsoft 或被管理应用无隶属、背书关系。

## 更新

- 窗口隐藏或最小化时，仅为托盘管理自身启用低优先级和 EcoQoS 节能调度。
- 打开窗口时恢复原进程优先级并关闭 EcoQoS，后台暂停手柄轮询。
- 托盘隐藏规则轮询和全局快捷键继续运行，不调整其他应用的优先级。
- 保留 68 DIP 列表、DPI 缩放修复、Xbox 大屏体验自动检测和标题栏适配。

32 项测试通过，包括效率模式启用与恢复的接口回读。任务管理器绿叶标识的实际显示未完成目视验证。

## 安装

下载 `TrayManager-1.1.8-win-x64.zip`，解压后运行 `Install.ps1` 或直接运行 `TrayManager.exe`。
升级前点击旧版窗口底部的“退出程序”。用户设置保留，不随安装包分发。
需要 Windows 11 x64、.NET 10 Runtime 和 Windows App Runtime 2；本包使用共享运行库。

## 已知限制

- OneDrive 等应用目前优先显示 Windows 缓存的托盘图标，必要时使用程序图标；可能出现状态图标或过时缓存。固定应用图标方案尚未实现，不应根据本列表判断同步或登录状态。
- 不保证枚举所有托盘图标；开关表示本工具规则，不是系统原有状态。
- 尚无正式卸载器或卸载自动恢复；删除程序前正常退出。异常结束可能无法恢复已隐藏图标。
