# 托盘管理 1.1.9 · 可选开机自启动

个人原创独立工具，按照自己的审美和使用习惯制作；不是其他软件的修改版或 Fork，没有对应“官方原版”。实验项目，与 Microsoft 及被管理应用无隶属或背书关系。

## 更新

- 新增“开机自启动”开关，默认关闭。开启后通过当前用户启动项，在登录 Windows 时静默后台运行。
- 自启动不弹出窗口；重复静默启动不会多开进程，也不会唤出已有窗口。手动启动仍可打开已有窗口。
- 保留 68 DIP 列表、Xbox 模式自动适配、后台效率模式和全局快捷键。

## 安装和覆盖升级

需要 Windows 11 x64、.NET 10 Runtime 和 Windows App Runtime 2。
下载 ZIP 并解压，点击旧版窗口底部的“退出程序”，然后运行新版 `Install.ps1`。无需卸载，会覆盖安装并启动，保留用户配置。
便携运行也可直接启动 `TrayManager.exe`；移动程序目录后需要重新设置自启动。自启动被任务管理器禁用时需在那里重新启用。

## 验证与限制

- 36 项测试通过，包括隔离注册表中的启动项添加、移除及路径引号测试。已验证静默启动、单实例以及手动唤出窗口。
- 收到过自启动后隐藏规则似乎未及时生效、随后自行恢复的反馈。检查时进程已通过后台参数启动；原因未确定，尚未修复，完整开机时序仍需验证。
- OneDrive 等图标仍可能来自状态缓存，不保证代表当前登录或同步状态。
- 尚无正式卸载器或卸载自动恢复。卸载前先关闭自启动，再正常退出；强制结束不能保证恢复隐藏图标。
- 安装包不含用户配置、隐藏规则或运行日志。
